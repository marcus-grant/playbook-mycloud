---
all:
  children:
    # homeservers:
    #   hosts:
    #     default:  # the vagrant testing server
    #       ansible_host: 127.0.0.1
    #       ansible_port: 2222
    #       ansible_user: vagrant
    #       become_method: sudo
    ocean:
      hosts:
        snori:  # the vagrant testing server
          ansible_host: 192.168.99.12
          # ansible_host: 127.0.0.1
          ansible_port: 22
          ansible_user: vagrant
          become_method: sudo
